<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/lunch_bg1"
    tools:context="site.yanhui.mobilesafe.SplashActivity">

<!--使用相对布局的时候，一定要注意，@id必须要先赋值，否则的话编译不过-->


    <TextView
        android:id="@+id/version_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:shadowColor="#ff0"
        android:shadowDx="1"
        android:shadowDy="1"
        android:shadowRadius="10"
        android:text="版本名称"
        android:textColor="#fff"
        android:textSize="20sp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/version_name"
        android:id="@+id/app_name"
        android:layout_centerHorizontal="true"
        android:text="手机卫士"
        android:textColor="#fff"
        android:textSize="30sp"/>

    <ProgressBar
        style="?android:attr/progressDrawable"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/version_name"
        android:layout_centerHorizontal="true"
        />

</RelativeLayout>
